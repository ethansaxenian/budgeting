[build]
pre_cmd = ["tailwindcss -i ./assets/main.css -o ./assets/dist/tailwind.css --minify"]
cmd = "templ generate --notify-proxy --proxyport=\"${APP_PORT}\""
bin = "/usr/bin/true"
include_dir = ["assets"]
include_ext = ["css"]
exclude_dir = ["assets/dist"]
delay = 100
